{
  "name": "watch",
  "version": "1.0.0",
  "entry": "./index.ts",
  "sdk": "^1.0.0",
  "description": "Activity worklog — desync-killer timeline (tool-calls + PR status).",
  "author": "kobo:core",
  "cli": {
    "command": "watch",
    "help": "DEPRECATED alias → use 'maw company worklog <log|inject|claim|release|sync|setup-hooks>' (this top-level 'maw watch' is removed next release)",
    "hidden": true
  },
  "hooks": {
    "serve": {
      "script": "./serve.ts",
      "handler": "serve",
      "ensures": ["http:route:/api/worklog", "http:route:/api/worklog/feed", "http:route:/api/tasks", "http:route:/api/state", "http:route:/api/policy"],
      "policy": "best-effort"
    }
  },
  "weight": 10,
  "license": "MIT",
  "schemaVersion": 1
}
